//Primitive data types
//String, Number, NULL, undefined, Boolean, BigInt, Symbol --- Primitive types or Non Reference Types
// Arrays, Functions and Objects --- Non Primitive Types or Reference Types

const id = Symbol('123');
const anotherId = Symbol('123');

console.log(id == anotherId);
console.log(id);